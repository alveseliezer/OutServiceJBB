<p id="notice"><%= notice %></p>

<h1>Tipo Servicos</h1>

<table>
  <thead>
    <tr>
      <th>Tipo servico</th>
      <th>Valorunitario</th>
      <th>Valortotal</th>
      <th>References</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_servicos.each do |tipo_servico| %>
      <tr>
        <td><%= tipo_servico.tipo_Servico %></td>
        <td><%= tipo_servico.valorUnitario %></td>
        <td><%= tipo_servico.valorTotal %></td>
        <td><%= tipo_servico.references %></td>
        <td><%= link_to 'Show', tipo_servico %></td>
        <td><%= link_to 'Edit', edit_tipo_servico_path(tipo_servico) %></td>
        <td><%= link_to 'Destroy', tipo_servico, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Servico', new_tipo_servico_path %>
